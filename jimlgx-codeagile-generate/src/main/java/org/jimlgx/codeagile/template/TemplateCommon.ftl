/*
 * @project ${projectName}
 * @package ${packages}
 * @file ${fileName}
 * @version  ${version}
 * @author  ${author}
 * @time  ${dateTime}
 * CopyRight:${copyRight}
 */
package ${packages};
/**
 * <code>${simpleName}</code> ${simpleName} 
 * //TODO 记得写注释啊！
 * @version  ${version}
 * @author   ${author}
 * @since    ${version} ${date}
 */
${modifiers}  class ${simpleName} {
}
